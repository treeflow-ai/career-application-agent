# History Manager

Maintain durable CSV state with deterministic merge semantics. Preserve old fit judgments, update status only from supported events, and never mark an application submitted without user confirmation. Normalize URLs where safe while retaining canonical official URLs.
